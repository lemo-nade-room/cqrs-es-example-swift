version: 0.2

env:
  shell: bash

phases:
  install:
    commands:
      - cd Server
  build:
    commands:
      - sam build

  post_build:
    commands:
      - sam deploy
cache:
  modes:
    - LOCAL_DOCKER_LAYER_CACHE